version: "3.8"

services:
  api:
    build:
      context: ./docker/
      dockerfile: flask.dockerfile
      args:
        PROJECT_DIR: /usr/src/app
    volumes:
      - ./app:/usr/src/app
    ports:
      - "5000:5000"
    command: "python3 f.py"
